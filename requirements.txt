# 서울 가로수 테마길 에이전트 — 실행 의존성
# 설치:  uv venv /root/venvs/rushhour && uv pip install --python /root/venvs/rushhour/bin/python -r requirements.txt
pandas>=2.0
pyarrow>=15              # data/processed/seoul_trees.parquet (scripts/01)
openpyxl>=3.1            # 원본 xlsx 재파싱용(선택)
langchain-core>=1.0
langgraph>=1.0
langgraph-checkpoint-sqlite>=3.0   # 스레드 체크포인트(HITL·복구)
langchain-openai>=1.0
pydantic-settings>=2.0   # app/config.py — .env + 환경변수 중앙 설정
openai>=1.0              # 로컬 서버 모델 id 조회에 사용
fastapi>=0.115           # backend/
uvicorn[standard]>=0.30
httpx>=0.27              # UI→API 클라이언트 · 카카오 지오코딩(tools) · /health 확인
streamlit>=1.63          # UI (pydeck 동봉)
pytest>=8                # tests/ (LLM 없이 실행 가능)
# 선택: langfuse>=3      # TRACE_BACKEND=langfuse 일 때만
