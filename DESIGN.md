# 최종 프로젝트 설계 — 서울 공공 버스 데이터 기반 착석 예측 시스템

작성: clabs4@scopelabs.site · 2026-09-11
전제 자료: 서울시 버스 승하차 데이터 3년치 (공공데이터포털 / 서울 열린데이터광장)

---

## 0. 이 문서를 읽는 법

1절이 **2~8주차에서 실제로 배운 것의 인벤토리**이고, 2절이 **그중 이 프로젝트에 안 쓰이는 것과
빠져 있는 것**입니다. 3절이 가장 중요합니다 — 자료가 무엇을 주고 무엇을 주지 않는지를 먼저
확정하지 않으면 4절 이후의 아키텍처가 전부 허공에 뜹니다. 4절부터가 설계이고, 마지막 16절이
6주차 `DECISIONS.md` 형식의 결정 지점 목록입니다.

**설계의 한 줄 요약**: 착석 예측의 계산은 배운 기술이 아니라 얇은 통계·정형 모델이 하고,
2~8주차에서 배운 것 전부는 그 예측을 **도구로 감싸고 · 설명하고 · 검색으로 보강하고 ·
평가하고 · 서빙하고 · 관측하는** 계층에 들어갑니다. 이 경계를 흐리면 시스템이 망합니다.

---

## 1. course 폴더에서 확인한 학습 기술 인벤토리

| 주차 | 주제 | 확보한 기술 (파일 기준) |
|---|---|---|
| **week2** | 프롬프트/ICL · 로컬 추론 | few-shot 개수 스윕 `ex01` · 프롬프트 A/B `ex02` · CoT `ex03` · HF 모델 수급 `ex04` · llama.cpp 로컬 서빙 `start_llama_server.sh` · 양자화 변형 KLD 비교 `ex07` · 컨텍스트 프로브 `ex06` · 속도·VRAM 실측 `ex08` · 한국어 프로브 `ex09` |
| **week3** | 서빙 엔진 · 구조화 출력 | vLLM 오프라인 배칭 `ex02` · 요청별 파라미터 `ex03` · 부하 생성과 꼬리 지연 `ex04` · 엔진 비교 `ex05` · SGLang RadixAttention 프리픽스 캐시 `ex06` · **JSON 스키마 강제 3단계 `ex07`** · **검증·재시도 루프 `ex08`** · **Function Calling `ex09`** · 통합 파이프라인 `ex10` |
| **week4** | RAG 전체 | 전처리·청킹 3계열 `ex01` · 임베딩 2채널(bge-m3 1024d / gemini 3072d) + Chroma 색인 `ex02` · **검색 평가 지표(recall@k·MRR·nDCG) `ex03`** · 청킹 ablation `ex04` · **BM25+벡터 RRF 융합 `ex05`** · LLM 리랭킹 `ex06` · 쿼리 변형 3종 `ex07` · LCEL 파이프라인 `ex08` · **종단 RAG + 출처 인용 `ex09`** · **검색실패/생성실패 분리 진단 `ex10`** · 자기교정 RAG `ex11` · 인덱스 운영 + RAG 보안 `ex12` |
| **week5** | 에이전트 | 손으로 만든 ReAct 루프 `ex01` · **도구 스키마가 곧 프롬프트 `ex02`** · **LangGraph StateGraph + ToolNode + 조건부 엣지 `ex03`** · **HITL 인터럽트 + 궤적 기록 `ex04`** · supervisor 멀티에이전트 `ex05` · 예산·종료조건 `ext01` · 그래프 위상 4종 `ext02` · 체크포인트 영속성 `ext03` · 관찰경유 인젝션 방어 `ext04` · 병렬 서브그래프 `ext05` · MCP `ext06` |
| **week6** | 통합 미드텀 | RAG 백엔드 + supervisor 그래프 종단 조립 · **"모르면 모른다고 말하는지"로 판정(DP8)** · **임계값은 eval셋으로 정하고 inbox는 마지막에 한 번(규율)** · 결정 근거 기록 `DECISIONS.md` · 결함 스니펫 감별 · 스키마 강제 티켓 `pj04` · 인젝션 방어 `pj05` · 승인 게이트 `pj06` |
| **week7** | 멀티모달 | VLM + mmproj `ex01` · 문서 추출 스키마 강제, **결측을 결측으로** `ex02` · **차트 판독 1단 vs 2단 + 재현성 `ex03`** · 파싱 라우팅 + 텍스트/이미지 2인덱스 `ex04` · SigLIP 이미지 임베딩 · 멀티모달 융합 검색 + 인용 `ex05` · 이미지 인젝션 `ext01` |
| **week8** | 파인튜닝 · 풀스택 서빙 | SFT 데이터 형식·챗템플릿·**응답부 마스킹** `ex01` · **LoRA/QLoRA(unsloth) 4bit vs 16bit 교환관계** `ex02` · **학습 전후 비교 평가** `ex03` · **FastAPI 추론 API(비스트리밍·SSE·폴백) `ex04`** · **Streamlit UI** `app_streamlit.py` · **Langfuse 트레이싱 + 3종 점수 `ext01`** · GRPO 보상함수와 보상 해킹 `ext02` · 어댑터 병합·서빙 `ext03` |

### 주차를 가로지르는 규율 — 기술보다 이것이 자산임

- **[예측후측정]** — 재기 전에 예측을 적는다 (week3 `ex02`·`ex06`, week4 `ex03`·`ex04`).
- **결과를 미화하지 않는다** — 융합이 지고, 쿼리 변형이 기준선을 못 넘고, 처방이 성공을 깎는
  결과를 그대로 남긴다 (week4 §7).
- **임계값은 평가셋으로 정하고, 최종 판정셋은 마지막에 한 번만 돌린다** (week6 §5 규율).
- **모르는 것을 모른다고 말하는 시스템이 자신 있게 틀리는 시스템보다 낫다** (week6 DP8).
- **환경 계층 분리** — 무거운 스택을 공용 venv에 들이지 않는다 (week3 §1, week6 §2.2, week8 §2).
- **포트 레지스트리 고정** · 쓰고 나면 끈다.
- **결정과 근거의 연결이 산출물의 본체다** (week6 §6).

---

## 2. 냉정한 진단 — 배운 것으로 덮이지 않는 구멍

`course/` 전체를 그렙으로 확인한 결과입니다.

```
xgboost | lightgbm | prophet | statsmodels | catboost | arima  →  0건
import pandas                                                  →  0건 (의존성엔 있으나 실습 사용 없음)
sklearn                                                        →  week4 환경 게이트의 임포트 확인용뿐
```

즉 **이 과정은 LLM 과정이고, 시계열·정형 회귀 예측은 한 번도 다루지 않았습니다.**
그런데 "착석 예측"의 본체는 정확히 그 영역입니다. 여기서 나올 수 있는 최악의 설계는
**LLM에게 숫자를 예측하게 시키는 것**입니다. 하지 말아야 하는 이유:

- 재현성이 없습니다 — week7 `ex03`에서 같은 차트·같은 질문에 답이 흔들리는 것을 직접 쟀습니다.
- 검증이 불가능합니다 — 모델이 왜 그 숫자를 냈는지 근거가 없고, RMSE를 개선할 손잡이가 없습니다.
- 비용이 3~4자리 배수로 비쌉니다 — 곱셈 한 번이면 되는 일에 27B를 돌립니다.

**따라서 예측 코어는 `pandas` + `scikit-learn`(이미 의존성에 있음)으로 별도 계층에 두고,
배운 것은 그 위·주변에 붙입니다.** 새로 배워야 하는 양은 전체의 15% 정도이며, 나머지 85%는
이미 가진 것으로 덮입니다.

---

## 3. 자료 현실 점검 — 설계 전에 반드시 먼저 할 일 (P0)

### 3.1 두 데이터셋은 서로 다른 것을 줍니다 (직접 확인할 것)

서울 열린데이터광장의 버스 승하차 자료는 계열이 둘입니다. 이름이 비슷해서 섞이기 쉽습니다.

| 계열 | 시간 해상도 | 날짜 해상도 | 주는 것 | 못 주는 것 |
|---|---|---|---|---|
| **A. 노선별 정류장별 시간대별 승하차** | **1시간 버킷 × 24** | **월 집계(YYYYMM)** | 하루 중 언제 몰리는지 | 요일·공휴일 효과 |
| **B. 노선별 정류장별 승하차** | 없음(일 합계) | **일자별(YYYYMMDD)** | 요일·공휴일·계절 효과 | 하루 중 시간 분포 |

**이것이 이 프로젝트 최대의 설계 제약이며 동시에 최대의 기회입니다.** 어느 쪽도 단독으로는
"화요일 오전 8시"를 주지 않습니다. 해법은 곱셈 분해입니다 — 4.2절에서 다룹니다.

> **P0에서 확인할 것**: 실제로 받은 3년치 파일이 A인지 B인지 둘 다인지, 컬럼명·인코딩
> (서울 공공데이터 CSV는 대개 **CP949**), 행 수, 결측·0 비율, 노선 개편으로 사라진 노선번호.
> 이 확인 없이 4절 이후를 구현하면 전부 다시 씁니다.

### 3.2 자료에 **없는** 것 — 그래서 추가로 구해야 하는 것

| 필요한 것 | 없으면 못 하는 계산 | 조달 경로 |
|---|---|---|
| **노선별 정류장 순서(경로)** | 누적 재차 인원 계산이 아예 불가능 | 공공데이터포털 "버스노선별 정류장 정보" / TOPIS |
| **시간대별 운행 횟수(배차)** | 1대당 승객 수 환산 불가 | 노선별 운행 정보 / 배차간격표 |
| **차종별 좌석 수** | 착석 여부 판정 기준이 없음 | 노선 유형별 가정표(3.4절) |
| **상·하행 방향 구분** | 같은 정류장의 반대 방향이 섞임 | 정류장 정보의 방향 필드 |

### 3.3 가장 중요한 사실 — **착석 라벨은 자료에 없습니다**

이 자료는 정류장 단위 승·하차 **인원 수**만 줍니다. 개별 차량의 재차 인원(occupancy)도,
좌석 점유도, "앉았는가"도 없습니다. 즉 **지도학습의 정답 라벨이 존재하지 않습니다.**

이것을 정면으로 다루는 것이 이 프로젝트의 성립 조건입니다. 선택지는 셋입니다.

- **(a) 추정 라벨(proxy)을 정의하고 그 가정을 전부 문서화한다** ← 이 설계의 선택
- (b) 문제를 바꾼다 — "착석 예측"이 아니라 "정류장 혼잡도·재차 인원 예측"
- (c) 실시간 API(버스 위치·재차)로 소규모 검증셋을 직접 수집해 proxy를 보정한다 (선택 확장)

week7 `ex02`가 가르친 "**결측을 결측으로 두기**"의 정신이 여기에 그대로 적용됩니다.
관측하지 않은 것을 관측한 것처럼 말하지 않습니다. 사용자에게 나가는 답에는 항상
"추정치이며 근거는 이러함"이 붙습니다.

### 3.4 추정 라벨 정의안

노선 `r`, 정류장 순서 `s`, 시간대 `h`에 대해:

```
구간 재차 인원      load(r,s,h) = Σ_{s'≤s} board(r,s',h) − Σ_{s'≤s} alight(r,s',h)
1대당 재차 인원     per_bus(r,s,h) = load(r,s,h) / trips(r,h)
착석 여유           margin(r,s,h)  = seats(r) − per_bus(r,s,h)
착석 확률 proxy     p_seat = clip(1 − per_bus / seats, 0, 1)
```

`seats(r)` 가정표 (노선 유형 기준, P0에서 실제 차종 자료로 교체할 것):

| 유형 | 좌석 수 가정 | 정원 가정 |
|---|---|---|
| 간선(파랑)·지선(초록) 일반 | 23 | 50 |
| 저상버스 | 19 | 45 |
| 광역(빨강)·좌석 | 41 | 45 |
| 마을(노랑) | 15 | 30 |

**이 proxy가 품고 있는 가정 — 반드시 문서와 UI에 함께 노출할 것:**

1. 시간대(1시간) 안에서 승객이 균등하게 도착한다 → 실제로는 8:00~8:20에 몰림
2. 그 시간대의 모든 운행 차량에 균등 분산된다 → 실제로는 앞차·뒤차 쏠림(bus bunching)
3. 월 집계를 일 단위로 나눠 쓴다 → 특정일 이벤트(집회·경기·폭우)는 표현 못 함
4. 방향이 섞였을 수 있다 → 3.2절 방향 필드로 해소
5. 좌석에 앉으려면 빈 좌석 존재 + 그 좌석까지 접근 가능해야 하는데 후자는 모델링 안 함

**분류로 바꾸는 편이 정직합니다.** 회귀값 하나를 "78% 확률로 앉음"으로 내보내면 없는 정밀도를
주장하게 됩니다. 3단계 등급을 권합니다:

```
여유(대체로 앉을 수 있음)   per_bus < 0.7 × seats
보통(앉을 수도 있음)        0.7 × seats ≤ per_bus < 1.0 × seats
혼잡(서서 갈 가능성 높음)   per_bus ≥ 1.0 × seats
```

---

## 4. 시스템 아키텍처

### 4.1 4계층 구조

```
┌─ L3  서빙 · UI · 관측  ────────────────────────────────────── week8 그대로 ─┐
│  FastAPI :9000 (ex04 A파트)   Streamlit :8501 (app_streamlit.py)            │
│  /predict  /ask  /ask/stream(SSE)  /health   폴백(한국어 안내, 스택 노출 금지)│
│  Langfuse 트레이스 + 3종 점수(규칙·LLM심판·사용자피드백)  ← ext01            │
└────────────────────────────────────────┬───────────────────────────────────┘
                                         │
┌─ L2  지식 · 추론 계층  ───────────── week3·4·5·6·7에서 배운 것 전부 ────────┐
│                                                                             │
│   질문 ──► router/supervisor (LangGraph StateGraph, week5 ex03/ex05)        │
│              │                                                              │
│              ├─► predictor    : predict_seat / compare_alternatives 호출    │
│              ├─► researcher   : search_bus_docs (RAG, week4 ex09)           │
│              └─► explainer    : 예측 JSON → 한국어 안내 + 근거 + 한계       │
│                                                                             │
│   도구 계층 (week5 ex02 스키마 원칙 · week3 ex07/ex08 구조화·재시도)        │
│   예산·종료조건(ext01) · 체크포인트(ext03) · 인젝션 방어(ex12/ext04)        │
│   저신뢰 → "모른다" 경로 (week6 DP8 계승)                                   │
└────────────────────────────────────────┬───────────────────────────────────┘
                                         │ 함수 호출(HTTP 아님, in-process)
┌─ L1  예측 코어  ──────────────────── ★ 새로 배우는 15% ★ ──────────────────┐
│  baseline : (노선,정류장,시간대,요일유형) 과거 중앙값  ← 반드시 먼저 만듦   │
│  model    : sklearn HistGradientBoostingRegressor (분류는 Classifier)       │
│  출력     : per_bus 추정 · 등급 · 신뢰구간(분위 회귀) · data_coverage       │
└────────────────────────────────────────┬───────────────────────────────────┘
                                         │
┌─ L0  데이터 · 피처 계층  ─────────────────────────────────────────────────┐
│  원시 CSV(CP949) 3년치 → parquet 변환 → 정류장 순서 조인 → 누적 재차 계산   │
│  → 시간프로파일(A) × 요일계수(B) 곱셈 분해 → 피처 테이블                   │
└────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 곱셈 분해 — 3.1절 제약의 해법

A(시간대별·월집계)와 B(일자별·시간없음)를 하나로 합칩니다.

```
추정치(r, s, 요일d, 시간h, 월m)
    =  일평균(r, s, m)                    ← B에서: 해당 월 일평균 승차
     × 요일계수(r, d, m)                  ← B에서: 요일 평균 / 전체 일평균
     × 시간프로파일(r, s, h, m)           ← A에서: 해당 시간대 / 24시간 합
```

- 세 항 모두 **같은 (노선, 정류장, 월)** 안에서 계산되므로 노선 규모 차이가 상쇄됩니다.
- 검증: A의 24시간 합과 B의 월 합이 일치하는지 대조 → 두 자료의 정합성 점검이 곧 P0 산출물.
- 이 분해 자체가 **베이스라인 모델**입니다. sklearn 모델은 이 베이스라인을 **이겨야만**
  채택합니다. week4 `ex05`에서 융합이 단일 검색기를 못 이긴 것을 그대로 겪을 각오를 할 것.

### 4.3 피처 목록 (L1 입력)

| 분류 | 피처 |
|---|---|
| 시간 | 시간대(0~23, sin/cos 인코딩) · 요일 · 공휴일 여부 · 월 · 연도 추세 |
| 공간 | 정류장 순서 비율(s/전체) · 기점 거리 · 환승역 인접 여부 · 자치구 |
| 노선 | 노선 유형(간선/지선/광역/마을) · 총 정류장 수 · 시간대 배차 수 |
| 이력 | 동일 (노선,정류장,시간대) 지난 3개월 중앙값 · 전년 동월 값 · 상류 정류장 누적 승차 |
| 파생 | 승차/하차 비율 · 구간 재차 증가율 |

---

## 5. 계층별 매핑표 — 배운 실습이 어디에 들어가는가

| 이 시스템의 구성요소 | 근거 실습 | 그대로 쓰는 것 / 바꿀 것 |
|---|---|---|
| FastAPI `/predict`·`/ask` + Pydantic 스키마 | `week8/ex04_serve_api_q.py` | 구조 그대로. `ChatRequest`→`PredictRequest` |
| SSE 스트리밍 답변 | `week8/ex04` B파트 | 그대로. `data: {json}\n\n` + `[DONE]` |
| 추론 서버 죽었을 때 폴백 | `week8/ex04` 폴백 절 | **예측 API는 LLM 없이도 숫자를 내야 함** — 폴백 시 LLM 설명만 생략하고 수치는 반환 |
| Streamlit 화면 | `week8/app_streamlit.py` | 노선·정류장·시간 선택 UI로 교체 |
| Langfuse 트레이스 + 점수 | `week8/ext01` | 규칙 점수 = 인용 유무 · LLM심판 = 설명 적합성 · 사용자 피드백 = 👍/👎 |
| 도구 스키마 설계 | `week5/ex02` | **만능 도구 금지.** 6절 카탈로그 |
| supervisor 그래프 · 조건부 엣지 | `week5/ex03`·`ex05` | 7절 |
| HITL 승인 게이트 | `week5/ex04` · `week6/pj06` | 이 시스템엔 부작용 있는 행위가 없으므로 **기본은 미적용**. 알림 발송 확장 시에만 |
| 예산·종료조건 | `week5/ext01` | 도구 호출 상한 6회 · 토큰 예산 · hop 상한 |
| 체크포인트(대화 이어가기) | `week5/ext03` | sqlite. "그럼 30분 뒤는?" 같은 후속 질문에 필요 |
| JSON 스키마 강제 | `week3/ex07` | 예측 결과·설명 출력 모두 |
| 검증·재시도 루프 | `week3/ex08` | 라우터가 낸 노선번호가 화이트리스트에 없으면 재시도 |
| Function Calling | `week3/ex09` · `week5/ex01` | 도구 호출 왕복의 기본기 |
| RAG 색인(bge-m3 + Chroma) | `week4/ex01`·`ex02` | 버스 정책·요금·환승·노선 안내 문서 대상 |
| BM25 + 벡터 RRF 융합 | `week4/ex05` | **노선번호·정류장명은 키워드 검색이 강함** → 융합이 실제로 이길 가능성이 높은 드문 사례 |
| LLM 리랭킹 | `week4/ex06` · `week6` DP5 | 안/밖 **분리** 목적으로(6주차에서 얻은 교훈) |
| 종단 RAG + 출처 인용 | `week4/ex09` | 모든 문서 기반 답에 출처 필수 |
| 검색실패/생성실패 분리 진단 | `week4/ex10` | 여기선 **3분할** — 자료없음 / 예측틀림 / 설명틀림 |
| 검색 평가 지표 | `week4/ex03` | 9절 (B) 평가 |
| 인덱스 운영·재색인 | `week4/ex12` | 노선 개편 시 재색인 정책 |
| 인젝션 방어 | `week4/ex12` · `week5/ext04` · `week6/pj05` | 사용자 질문 정규화 + 도구 파라미터 화이트리스트 |
| "모르면 모른다" 판정 축 | `week6` DP8 | **이 프로젝트의 축.** 3.3절과 직결 |
| 결측을 결측으로 | `week7/ex02` | `data_coverage` 필드 · 표본 부족 시 등급 대신 "판정 불가" |
| 차트 판독(선택) | `week7/ex03` | 월간 리포트 차트 자동 검증 |
| QLoRA SFT(선택) | `week8/ex01`~`ex03` | 12절 |
| 양자화 변형 선택 | `week2/ex07`·`ex08` | 설명 생성용 모델 크기·변형 결정 근거 |
| 꼬리 지연 측정 | `week3/ex04` | `/predict` p95 목표 설정 |
| 프리픽스 캐시 | `week3/ex06` | 시스템 프롬프트 고정 → 캐시 이득 (측정해 볼 것) |

---

## 6. 도구 카탈로그 — week5 ex02 원칙 적용

`ex02`에서 **파라미터 스키마를 범용 `query` 하나로 뭉개면 정확도가 1.000 → 0.417로 떨어지는
것**을 직접 쟀습니다. 그래서 역할별 파라미터를 분리합니다.

```python
predict_seat_availability(
    route_no: str,          # 노선번호. 예: "143"
    stop_ars: str,          # 정류장 ARS 번호 5자리. 정류장명이 아님
    hour: int,              # 0~23
    day_type: Literal["weekday","saturday","holiday"],
) -> SeatPrediction         # 8절 스키마

compare_alternatives(
    stop_ars: str,          # 출발 정류장
    hour: int,
    day_type: str,
    candidate_routes: list[str],   # 그 정류장을 지나는 노선들
) -> list[SeatPrediction]   # 착석 여유 순 정렬

find_better_time(
    route_no: str, stop_ars: str, day_type: str,
    around_hour: int, window: int = 2,
) -> list[SeatPrediction]   # ±window 시간 중 가장 여유로운 시각

lookup_stop(query: str) -> list[StopMatch]   # 정류장명/지명 → ARS 번호 (모호하면 후보 반환)

search_bus_docs(question: str) -> list[Citation]   # RAG. 요금·환승·정책 등 수치 예측이 아닌 질문
```

**금지 사항 (`ex02` ③ 만능 도구의 교훈)**: `bus_query(query: str)` 같은 도구 하나로
전부 처리하려 하지 않습니다. `lookup_stop`을 따로 두는 이유도 같습니다 — 정류장 식별은
예측과 성격이 다른 작업이고, 모호할 때 후보를 되돌려 되묻는 경로가 필요합니다.

---

## 7. LangGraph 그래프 설계

```
                        ┌──────────────┐
   질문 ───► normalize ─►│  supervisor  │◄──── 담당자 종료 시 복귀
            (정규화·     └──┬───┬───┬───┘
             인젝션 차단)    │   │   │
                            │   │   └──────► researcher ─► search_bus_docs
                            │   │                              (RAG + RRF + 리랭킹)
                            │   └──────────► predictor  ─► predict_seat / compare /
                            │                              find_better_time / lookup_stop
                            └──────────────► explainer  ─► 한국어 안내 + 근거 + 한계
                                                 │
        ┌────────────────────────────────────────┤
        ▼                                        ▼
   [판정 불가 경로]                            END
   data_coverage 부족 · 노선 미존재 ·
   도구 예산 초과 → "그 구간은 자료가 부족해 판정하지 않음" + 이유
```

**상태(State) 설계** (week5 `ex03` DP 상태 설계):

```python
class S(TypedDict):
    messages: Annotated[list, add_messages]
    route_no: str | None
    stop_ars: str | None
    hour: int | None
    day_type: str | None
    predictions: list[dict]      # 도구가 채움
    citations: list[dict]        # RAG가 채움
    coverage: float | None       # 표본 충분도 0~1
    tool_calls_used: int         # 예산 (ext01)
    verdict: Literal["ok","insufficient_data","out_of_scope"] | None
```

**울타리 (week5 ext01)**: 도구 호출 6회 · hop 8회 상한. 걸리면 예외가 아니라
**"판정 불가" 경로로 흘려보냅니다** — 6주차에서 "울타리에 걸려도 티켓"으로 배선한 것과 같은
방향입니다. 여기서는 "울타리에 걸려도 정직한 실패".

**단일 에이전트로 시작할 것.** week5 `ex05`에서 팀이 단일보다 **토큰 1.4배·호출 3배**를 쓴 것을
쟀습니다. supervisor 분할은 단일 라우터로 부족하다는 것이 측정으로 확인된 뒤에만 도입합니다.

---

## 8. 구조화 출력 스키마 (week3 ex07/ex08 · week7 ex02)

```python
class SeatPrediction(BaseModel):
    route_no: str
    stop_name: str
    stop_ars: str
    hour: int
    day_type: str

    grade: Literal["여유", "보통", "혼잡", "판정불가"]
    per_bus_estimate: float | None      # 1대당 추정 재차 인원
    seats_assumed: int                  # 가정한 좌석 수 — 반드시 노출
    seat_margin: float | None           # seats - per_bus

    confidence: Literal["high", "medium", "low"]
    data_coverage: float                # 0~1. 근거가 된 월 표본 비율
    n_months_observed: int

    basis: str                          # "2023-01~2025-12 월집계 36개월 중 34개월"
    caveats: list[str]                  # 3.4절 가정 중 이 답에 걸리는 것
```

**핵심 규칙 세 개:**

1. `grade`가 `판정불가`면 `per_bus_estimate`는 **`None`이어야 하고 0이 아닙니다.**
   (week7 `ex02` — 결측을 0으로 채우면 그 자체가 거짓말)
2. `seats_assumed`와 `caveats`는 **생략 불가**. 추정치임을 답변마다 드러냅니다.
3. 검증 실패 시 재시도는 `week3/ex08`의 루프를 그대로 씁니다 — 2회 재시도 후에도 스키마가
   깨지면 `판정불가`로 강등하고 기록합니다.

---

## 9. 평가 설계 — 두 개를 분리해서 잽니다

week4 `ex10`이 가르친 "검색 실패와 생성 실패를 분리하라"의 확장입니다. 여기선 **3분할**:
① 자료가 없다 ② 예측이 틀렸다 ③ 설명이 틀렸다. 지표도 따로 둡니다.

### (A) 예측 정확도 — L1 계층

- **분할은 반드시 시간 기준.** 3년치 중 앞 30개월 학습 / 마지막 6개월 홀드아웃.
  **랜덤 분할 절대 금지** — 미래 정보가 새어 들어가 점수가 거짓으로 오릅니다.
- 지표: `per_bus`에 대한 MAE · RMSE · MAPE(0 근처 주의), 등급 분류의 macro-F1 · 혼동행렬.
- **혼잡 등급의 recall을 우선**합니다. "여유"라고 했는데 혼잡한 것이 반대 오류보다 나쁩니다.
- **베이스라인 대비 개선율을 반드시 병기**합니다. 4.2절 곱셈 분해를 못 이기면 모델을 버립니다.
- 드리프트 확인: 코로나 시기·노선 개편이 3년 안에 있으면 그 구간을 따로 잽니다.

### (B) 답변 품질 — L2·L3 계층

- **골든셋 40문항을 손으로 만듭니다** (week4 `golden/golden_set.json` 형식).
  구성: 예측 질문 15 · 문서 질문 10 · **자료 밖 질문 10**(존재하지 않는 노선, 미래 특정일,
  타 도시) · 애매한 정류장명 5.
- 지표: 도구 선택 정확도 · 인용 정확도 · **거부 정확도(자료 밖 10문항에서 "판정불가"를 냈는가)**.
- 거부 정확도가 이 프로젝트의 **판정 축**입니다 (week6 DP8). 예측 MAE가 좋아도 없는 노선에
  자신 있게 답하면 실패로 봅니다.
- LLM 심판 + 규칙 점수를 Langfuse에 남깁니다 (week8 `ext01`).

### 규율 (week6 §5 — 가장 지키기 어려운 부분)

- 임계값(0.7·1.0 계수, confidence 경계, coverage 하한)은 **검증셋으로만** 정합니다.
- **홀드아웃 6개월과 골든셋 40문항은 마지막에 한 번만 돌립니다.** 여기에 맞춰 손잡이를
  움직이면 그 숫자는 아무것도 뜻하지 않습니다.

---

## 10. 관측과 운영

| 항목 | 방법 | 근거 |
|---|---|---|
| 트레이스 | Langfuse — 그래프 노드·도구 호출·RAG 검색이 한 나무에 | `week8/ext01` |
| 점수 | 규칙(인용 유무) · LLM 심판(설명 적합성) · 사용자 👍/👎 | 같음 |
| 지연 | `/predict` p95, `/ask` TTFT를 부하 생성으로 실측 | `week3/ex04`, `week8/ex04` |
| 예측 로그 | 요청·응답·grade·coverage를 jsonl로 적재 | `week5/ex04` 궤적 |
| 드리프트 | 월별 승차 총량·시간 프로파일 변화 추적, 임계 초과 시 재학습 | `week4/ex12` 인덱스 운영 |
| 재색인 | 노선 개편 시 문서 인덱스 + 정류장 순서 테이블 동시 갱신 | 같음 |

---

## 11. 보안

- **인젝션 방어** — 사용자 질문은 `normalize` 노드에서 정규화 후 그래프에 들어갑니다.
  week6 `pj05`에서 확인한 대로, 막아 주는 것은 방어 프롬프트가 아니라 **구조**입니다:
  자유 텍스트가 도구 파라미터로 직접 흘러 들어가지 않게 합니다.
- **도구 파라미터 화이트리스트** — `route_no`는 실제 노선번호 집합에, `stop_ars`는 실제 ARS
  집합에 있어야 합니다. 없으면 도구를 부르지 않고 `판정불가`.
- **week4 `ex12`의 정직한 결과를 기억할 것** — 방어 프롬프트는 뚫렸습니다. 프롬프트를 믿지 않습니다.
- 개인정보: 이 자료는 집계치이므로 PII 없음. 다만 사용자 질의 로그에 위치 이력이 쌓이므로
  보관 기간을 정합니다.

---

## 12. 파인튜닝을 쓸 자리와 쓰지 말 자리 (week8)

**쓰지 말 자리**: 수치 예측. 어떤 이유로도 여기에 LLM을 넣지 않습니다 (2절).

**쓸 자리 (선택 확장)**: `SeatPrediction` JSON → 한국어 한 문장 안내 생성기.

- 왜 후보가 되는가: 출력 형태가 고정적이고 반복적이며, 소형 모델로 내려갈 수 있고,
  프롬프트로는 말투와 caveat 포함 여부가 흔들립니다.
- **먼저 프롬프트로 충분한지 확인할 것** (week2 `ex02` 프롬프트 A/B 규율). 프롬프트로
  해결되면 학습하지 않습니다.
- 하기로 정했다면: 27B로 1,500~2,000건 생성 후 사람이 검수 → `week8/ex01` 형식으로 대화
  변환 → **응답부만 마스킹** → `ex02`의 QLoRA 설정으로 4B 학습 → **`ex03`의 학습 전후 비교로
  개선을 증명**합니다. 증명 못 하면 어댑터를 버립니다.
- 시간이 없으면 통째로 생략하십시오. 시스템 가치의 대부분은 L0~L1과 평가에 있습니다.

---

## 13. VLM을 쓸 자리 (week7) — 선택

- **월간 리포트 검증**: 시스템이 그린 차트를 VLM에게 판독시켜 수치가 일치하는지 대조.
  단 `ex03`에서 확인한 재현성 문제가 있으므로 **3회 반복 후 다수결**, 불일치 시 사람에게.
- **노선도 이미지 파싱**: 정류장 순서를 이미지로만 얻을 수 있을 때. 텍스트 자료가 있으면 쓰지 않습니다.
- 8080 VRAM을 VLM이 점유하므로 텍스트 27B와 동시 운용 불가 (week7 §2). 오프라인 배치로 돌립니다.

---

## 14. 환경 · 포트 · 저장소 구조

### 환경 (week3 §1 · week6 §2.2 계층 분리 계승)

```bash
# 프로젝트 전용 venv — course venv(pyc)를 상속해 복제
bash week6/setup_midterm_venv.sh 를 본떠 final_prj/setup_venv.sh 작성
# 추가 패키지: pyarrow, scikit-learn(이미 있음), holidays
# 별칭: pyf = /root/venvs/final/bin/python
```

무거운 것을 `pyc`에 넣지 않습니다. sklearn·pyarrow는 가벼우므로 프로젝트 venv에 둡니다.

### 포트 (레지스트리 준수 — 8081·8001·3001·7270·7861·9091은 Runpod nginx 소유)

| 포트 | 용도 |
|---|---|
| 8080 | llama.cpp 설명 생성 모델 (`week5/start_agent_server.sh` 또는 `--small`) |
| 8082 | bge-m3 임베딩 (`week4/start_embed_server.sh`) |
| 9000 | 이 프로젝트 FastAPI |
| 8501 | Streamlit UI |

L1 예측 모델은 **별도 포트를 열지 않고 FastAPI 프로세스에 in-process 로드**합니다.
곱셈 한 번짜리 계산에 네트워크 왕복을 넣지 않습니다.

### 저장소 구조

```
final_prj/
├── DESIGN.md                  ← 이 문서
├── DECISIONS.md               ← 제출물 본체. 결정과 근거 (week6 형식)
├── setup_venv.sh
├── data/
│   ├── raw/                   원시 CSV (git 제외)
│   ├── parquet/               변환본
│   ├── stop_sequence.csv      노선별 정류장 순서 (외부 조달)
│   └── trips_by_hour.csv      시간대 운행 횟수 (외부 조달)
├── golden/
│   ├── golden_set.json        답변 품질 40문항
│   └── eval_queries.jsonl     임계값 튜닝용 (홀드아웃과 분리)
├── p0_audit.py                자료 실사 — 스키마·결측·정합성
├── p1_features.py             L0: 변환·조인·누적재차·곱셈분해
├── p2_baseline.py             L1: 베이스라인 + 지표
├── p3_model.py                L1: sklearn 모델 + 시간분할 평가
├── p4_tools.py                L2: 도구 5종 + Pydantic 스키마
├── p5_graph.py                L2: LangGraph 그래프
├── p6_api.py                  L3: FastAPI (week8/ex04 구조)
├── app_streamlit.py           L3: UI
├── p7_eval.py                 (A)(B) 평가 종단 실행기
└── results/                   week08_ 접두사 관례처럼 final_ 접두사
```

---

## 15. 단계별 진행 계획

| 단계 | 내용 | 산출물 | 완료 판정 |
|---|---|---|---|
| **P0** | 자료 실사 — 3.1·3.2 확인. A/B 계열 판별, 인코딩, 결측, A24합 vs B월합 정합성 | `p0_audit.py` 출력 · 결측표 | **없는 것 4종의 조달 경로가 확정됨** |
| **P1** | L0 — parquet 변환, 정류장 순서 조인, 누적 재차, 곱셈 분해 | 피처 테이블 · `seats` 가정표 | 한 노선을 손으로 검산해 값이 맞음 |
| **P2** | L1 베이스라인 — 과거 중앙값 + 곱셈 분해, 시간분할 평가 | MAE/F1 기준선 숫자 | **이 숫자가 앞으로 모든 비교의 기준** |
| **P3** | L1 모델 — HistGradientBoosting, 분위 회귀로 구간 | 개선율 표 | 베이스라인을 이겼는가 (못 이기면 버림) |
| **P4** | L2 도구 5종 + Pydantic 스키마 + 검증·재시도 | `p4_tools.py` | 도구 선택 정확도 측정 (week5 ex02 방식) |
| **P5** | L2 그래프 — 단일 라우터로 먼저. 판정불가 경로 | `p5_graph.py` | 자료 밖 질문 10건에서 판정불가가 나오는가 |
| **P6** | L3 FastAPI + SSE + 폴백 + Streamlit + Langfuse | `p6_api.py` · UI | `/health`·폴백·TTFT 실측 |
| **P7** | 평가 — (A) 홀드아웃 1회 · (B) 골든셋 1회 · DECISIONS 마무리 | `results/final_report.json` | 결정과 근거가 전부 연결됨 |
| **선택** | 12절 QLoRA · 13절 VLM · MCP 서버화(`week5/ext06`) | | 개선 증명 실패 시 폐기 |

**우선순위가 흔들릴 때의 기준**: P0~P2와 P7이 이 프로젝트의 본체입니다. P3의 모델이 화려하지
않아도, P0의 실사와 P2의 기준선과 P7의 정직한 평가가 있으면 완성된 프로젝트입니다.
6주차 채점 기준 그대로입니다 — **근거 없는 높은 숫자보다 근거가 분명한 낮은 숫자가 낫습니다.**

---

## 16. 결정 지점 — DECISIONS.md에 채울 것

각 항목은 week6 형식(`[앞에서 넘어온 것]` → `[예측]` → `[해 보니]` → `[그래서 다음]`)으로
적습니다. 재기 전에 예측을 먼저 적습니다.

| # | 결정 지점 | 핵심 질문 | 참고 |
|---|---|---|---|
| **DP1** | 자료 계열 | A(시간대·월집계)만 · B(일자별)만 · 둘 다 곱셈 분해 중 무엇 | 3.1 · 4.2 |
| **DP2** | 라벨 정의 | 착석 proxy를 정의할 것인가, 문제를 혼잡도 예측으로 바꿀 것인가 | 3.3 |
| **DP3** | 좌석 수 가정 | 노선 유형 가정표로 갈 것인가, 차종 자료를 구할 것인가 | 3.4 |
| **DP4** | 출력 형태 | 연속 확률 vs 3등급 — 없는 정밀도를 주장하지 않는 쪽 | 3.4 |
| **DP5** | 예측 코어 | 베이스라인만으로 충분한가, 모델이 이겼는가 | P2·P3 |
| **DP6** | 시간 분할 | 홀드아웃 기간과 드리프트 구간 처리 | 9(A) |
| **DP7** | 도구 계약 | 도구를 몇 개로 쪼갤 것인가, 파라미터 분리 수준 | 6 · week5 ex02 |
| **DP8** | 라우팅 | 단일 라우터 vs supervisor — 토큰 1.4배를 지불할 근거가 있는가 | 7 · week5 ex05 |
| **DP9** | **판정 불가 임계값** | coverage 하한과 confidence 경계를 어디에. **이 프로젝트의 축** | 9(B) · week6 DP8 |
| **DP10** | RAG 필요성 | 문서 검색 계층이 실제로 필요한가, 예측만으로 충분한가 | 5 |
| **DP11** | 융합 | 노선번호·정류장명에 BM25+RRF가 이기는가 (4주차와 반대 결과 기대) | week4 ex05 |
| **DP12** | 파인튜닝 | 프롬프트로 충분한가. 어댑터가 개선을 증명했는가 | 12 |

---

## 17. 이 설계가 의도적으로 배제한 것

배제도 결정입니다. 이유를 적어 둡니다.

- **LLM에 의한 수치 예측** — 재현성·검증성·비용 전부 불리 (2절).
- **딥러닝 시계열(LSTM/Transformer 예측기)** — 이 과정에서 배우지 않았고, 월 집계 해상도의
  자료에 쓸 근거가 없습니다. 트리 기반이 정형 데이터에서 더 강하고 설명도 쉽습니다.
- **실시간 재차 인원 연동** — 자료 범위 밖입니다. 3.3(c)로 선택 확장에만 남깁니다.
- **HITL 승인 게이트 기본 적용** — 이 시스템에는 되돌릴 수 없는 부작용 행위가 없습니다.
  알림 발송·예약 같은 기능을 붙일 때만 `week5/ex04`를 꺼냅니다.
- **멀티에이전트 기본 적용** — 단일로 부족함이 측정될 때까지 하지 않습니다 (week5 §9).
- **CodeAct** — 5주차가 세운 원칙 그대로, 샌드박스가 없으면 하지 않습니다.
